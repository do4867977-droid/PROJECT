# Neon PostgreSQL (pooled connection string recommended)
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Wompi payment link (redirect after reservation)
WOMPI_PAYMENT_URL=https://checkout.wompi.co/l/VPOS_LBTnN9

# Wompi private key (API transactions, optional)
WOMPI_PRIVATE_KEY=your_wompi_private_key

# Redirect URL after payment (Wompi, optional)
REDIRECT_URL=http://localhost:3000/

PORT=3000
